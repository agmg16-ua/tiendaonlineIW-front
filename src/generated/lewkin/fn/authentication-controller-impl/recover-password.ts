/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { PasswordRecoveryRequest } from '../../models/password-recovery-request';
import { SendPasswordRecoverMailResponse } from '../../models/send-password-recover-mail-response';

export interface RecoverPassword$Params {
  token: string;
      body: PasswordRecoveryRequest
}

export function recoverPassword(http: HttpClient, rootUrl: string, params: RecoverPassword$Params, context?: HttpContext): Observable<StrictHttpResponse<SendPasswordRecoverMailResponse>> {
  const rb = new RequestBuilder(rootUrl, recoverPassword.PATH, 'post');
  if (params) {
    rb.query('token', params.token, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<SendPasswordRecoverMailResponse>;
    })
  );
}

recoverPassword.PATH = '/api/v1/auth/recover';
